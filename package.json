{
  "name": "bhavya-portfolio-root",
  "version": "1.0.0",
  "description": "Root workspace for Bhavya Sree Jallolla Personal Portfolio",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "server": "node server/server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.7.0",
    "morgan": "^1.11.0"
  }
}
